const Product = require("../models/product.model");
class specific {
    index(req, res, next) {
        const ProductInformation = { gender, type, name }
        res.render("specific")
    }
}
module.exports = new specific();